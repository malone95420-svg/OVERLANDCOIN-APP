"use client";

/**
 * Legacy PresaleLock UI — retired from product surfaces.
 * Contract stays on-chain (do not redeploy / do not enableTrading for test dust).
 * Test locked balances (~dust) abandoned in the lock; new buys deliver OLC to wallet.
 */
export function LockedPresaleOlC() {
  return null;
}
